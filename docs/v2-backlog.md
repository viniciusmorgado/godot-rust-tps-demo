# v2 Backlog

Improvements noticed during v1 (raw port) and deliberately NOT applied, per Principle I of the
constitution. One entry per improvement: origin (script/scene) and motivation. Evaluated one by
one as v2 milestones land; the `Status` column tracks that — `open` until a milestone closes the
item, then `done` citing the closing commit(s) (constitution 1.4.0, Principle I, "the backlog is
CONSUMED").

**v2 complete on 2026-09-18** (milestones V2-A…V2-E, specs 006–010); open items are post-v2
candidates.

| # | Origin | Improvement | Motivation | Status |
|---|---|---|---|---|
| 1 | `docs/port-order.md` (initial analysis); consumers: forklift, bullet, level, menu, main | Typed access to the `Settings` autoload (`Gd<Settings>`) instead of `get_node("/root/Settings").get("config_file")` and local integer constants mirroring the enums | In v1 `Settings` is ported last, so the Rust consumers are born with dynamic access | done (`eb1bb12`, milestone V2-A) |
| 2 | `player/bullet/bullet.gd` | Replace the dynamic `has_method("hit")` + `rpc("hit")` with a hittable-target trait/enum (`Hittable`) | Duck typing inherited from GDScript; in Rust it becomes untyped dispatch | done (32d6734, milestone V2-C) |
| 3 | `main/main.gd` | Replace `has_signal("quit")`/`has_signal("replace_main_scene")` with a centralized signal-driven scene manager | Declared goal of v2 in the constitution; today it is duck typing over the root node of the loaded scene | done (21780d5, milestone V2-E) |
| 4 | `level/debug.gd` (port 1) | Do not recompute the text while the overlay is hidden | Unnecessary per-frame work; the original (and v1, for fidelity) rebuilds the text every frame even with `visible = false` | done (`f1dc548`, milestone V2-B) |
| 5 | `enemies/red_robot/parts/part_disappear_effect/part_disappear.gd` and `enemies/red_robot/laser/impact_effect/blast.gd` (ports 2–3) | Timers/signals as `async` (`godot::task`) once the API stabilizes | The chain of `connect_other` closures reproduces GDScript's `await` in a less readable way | done (`8df3388`) |
| 6 | `player/camera_noise_shake_effect.gd` (port 4) | Recapture `start_rotation` when animations/scripts move the camera | The original's comment admits the problem; the shake adds to the rotation captured once in `_ready` | open — deferred in V2-B: needs a decision on who owns the camera's rest rotation across effects (spec 007 top block); post-v2 review candidate |
| 7 | `player/player_input.gd` (port 5) | Actually exclude the player's body in the raycast (`exclude` with the RID of the parent `CharacterBody3D`) | The original passes `Array([self], TYPE_RID, "", null)` = `[RID(0)]` (the synchronizer is not a physics body); ineffective exclusion, preserved in v1 | done (`c27365f`) |
| 8 | `player/player_input.gd` (port 5) | `OnEditor<Gd<T>>` instead of `Option<Gd<T>>` for the 6 mandatory references (`camera_animation`, `crosshair`, `camera_base`, `camera_rot`, `camera_camera`, `color_rect`) | Removes the per-frame `unwrap()` and makes the editor flag a missing reference | done (`c27365f`) |
| 9 | `player/player_input.gd` (port 5) | Replicate `jumping` or remove the `@export` | Exported but outside the `SceneReplicationConfig`; only makes sense via the `jump` RPC | done (`c27365f`) |
| 10 | `player/player.gd` (port 1) | Start `airborne_time` at 0 (or ignore the first landing) | With an initial 100, the first contact with the floor fires `land` and the landing sound on spawn; kept in v1 for fidelity | done (c3e8d8b) — note: beyond removing the spawn land sound, starting at 0 makes the first frame Walk instead of JumpDown, so the AnimationTree's root motion begins ~1–2 frames earlier after spawn (verified: with the v1 initial value restored, v2 matches v1 frame for frame) |
| 11 | `player/player.gd` (port 1) | Zero `velocity` on respawn below −40 | The teleport to the initial position preserves the accumulated fall velocity; kept in v1 for fidelity | done (c3e8d8b) |
| 12 | `player/player.gd` (port 1) | Remove the never-read `crosshair` reference (or use it) | Declared in `player.gd:30` and read nowhere; kept in v1 for fidelity | done (c3e8d8b) |
| 13 | `player/bullet/bullet.gd` (port 2) | Avoid the double `explode` when `time_alive` expires and there is a collision in the same frame | Two `explode` RPCs in the same frame restart the animation; kept in v1 for fidelity | done (32d6734) — verified by code review + the pure step test; the same-tick expiry+collision case WAS reproduced deterministically in the parity harness (calibrated wall placement, natural expiry frame 300 on both `v1`/`v2`, a real collision confirmed by the bullet stopping short of its unobstructed final position) — matches on both trees; the one-vs-two `explode` RPC count itself is not independently observable from GDScript (`AnimationPlayer.play()` on an already-playing clip is a no-op) |
| 14 | `door/door.gd` (port 3) | Type `_on_door_body_entered` with `Gd<Player>` right at the boundary (`try_cast` on the signal) instead of `Gd<Node3D>` + `try_cast` in the body | The original's `body is Player` becomes a `try_cast` inside the handler; kept in v1 for fidelity | done (5180793) — revised form: handler keeps Gd<Node3D>, boundary try_cast feeds a pure decision |
| 15 | `enemies/red_robot/parts/part.gd` (port 1) | Instantiate the puff under the **robot's** parent (or the root) instead of the part's parent (`Death`) | The puff is born under the robot, which is removed 10 s after death; today the timings do not cross (the fade ends in ≤ 6.7 s), but the dependency is fragile; kept in v1 for fidelity | done (b855ad6, milestone V2-D) |
| 16 | `enemies/red_robot/red_robot.gd` (port 2) | Remove the dead `body.name == "Target"` branch and type `player` as `Gd<Player>` | No scene has a `Target` node; the generic reference (`Node3D`) forces a `try_cast` on every use; kept in v1 for fidelity | done (035604d) |
| 17 | `enemies/red_robot/red_robot.gd` (port 2) | Replace the 10 s `await` inside the `hit` RPC with a timer/signal outside the RPC | The robot's removal is coupled to the damage handler; kept in v1 for fidelity | done (035604d) |
| 18 | `enemies/red_robot/red_robot.gd` (port 2) | Replicate `aim_preparing` (or do not export it) and take `test_shoot` out of the inspector | Exported but outside the `SceneReplicationConfig`; `test_shoot` is an internal trigger of the method track; kept in v1 for fidelity | done (035604d) |
| 19 | `level/forklift/flying_forklift.gd` (port 1) | Do not call `randomize()` per instance (`Main` already re-seeds at boot) | Re-seeding the global generator per forklift is redundant and makes the draw depend on the clock at each spawn; kept in v1 for fidelity | done (a54f79e forklift, a86b13f level.rs pre-shuffle randomize — same redundancy, closed together) |
| 20 | `level/level.gd` (port 2) | Make the spawns' `add_child` uniform: `add_child(player, true)` as for the robot, or `spawn_robot` without a readable name | The two spawns use different forms of `add_child`; it works because `name = str(id)` is already unique; kept in v1 for fidelity | done (a86b13f) |
| 21 | `level/level.gd` (port 2) | Expose `add_player`/`del_player` with the default parameter via two `#[func]`s (or remove the default) | gdext has no default parameters in `#[func]`; v1 connects `peer_connected`/`peer_disconnected` through a typed closure, which changes the form (not the effect) of the connection | done (a86b13f) |
| 22 | `menu/menu.gd` (port 3) | Declarative option→value table instead of ~30 `if/elif` chains in `_on_settings_pressed`/`_on_apply_pressed` | ~250 repetitive lines; every new option requires editing two places; kept in v1 for fidelity | done (c667e7c) |
| 23 | `menu/menu.gd` (port 3) | Declare `replace_main_scene` with the parameter in the original (already done in the port) and connect `quit`/`replace_main_scene` in `Main` by type instead of `has_signal` | Inherited duck typing; after v1 every scene is a native class | done (21780d5) |
| 24 | `main/main.gd` (port 4) | Call `change_scene_to_packed` directly instead of `call_deferred` by name | String call to its own method; kept in v1 for fidelity | done (21780d5) |
| 25 | `menu/settings.gd` (port 5) | Review the SSAO mapping: "Medium" applies `ENV_SSAO_QUALITY_HIGH` without `half_size` and "High"/others apply `MEDIUM` with `half_size` (SSIL does the opposite, consistent with the names) | Ambiguous upstream intent (swapped names or constants); v1 preserves it for fidelity — only the "Disabled" `if`→`elif` was fixed (upstream-bugs #3) | open — parity first: milestone V2-A reproduced both quirks verbatim in `plan()` (`settings/graphics.rs`), still deferred; post-v2 review candidate |
| 26 | `level/debug.gd` (port 1, `debug_label.rs`) — user request on 2026-09-16 during the Milestone E validation | Also show VRAM in the overlay (`RenderingServer.get_rendering_info(RENDERING_INFO_VIDEO_MEM_USED)`, e.g. `Memory: 117.74 MiB RAM / 10.04 MiB VRAM`) | The current `Memory` line is RAM (`OS.get_static_memory_usage()`), not VRAM; it is an improvement (forbidden in v1), authorized by the user for v2. Note: "VSync: Enabled" with the Disabled option is NOT a bug — on Wayland the immediate mode does not exist and the engine warns "Falling back to V-Sync mode Enabled"; the overlay shows the effective mode (on X11 it shows Disabled) | done (`f1dc548`, milestone V2-B) |
| 27 | `menu/settings.gd` → `settings.rs`/`settings/graphics.rs` (milestone V2-A) | Parse, don't validate: an out-of-range or unsupported-type value for a key in `user://settings.ini` falls back to that key's default with a `godot_warn!`, instead of falling through whichever `else` branch happens to match | `v1` has no validation at the wire boundary, so a hand-edited or corrupted file silently produces an ambiguous, unintended state; this is the one deliberate behavior deviation of milestone V2-A (spec 006, US1 scenario 9) | done (`097e14a` out-of-range codes, `eb1bb12` unsupported Variant types) |
| 28 | `enemies/red_robot/red_robot.rs::shoot` / `player.rs` — user observation during the V2-B checkpoint 2, 2026-09-16 | Investigate the ~10-frame hitch on the FIRST laser shot of every firing sequence (absent on subsequent shots; returns after a pause). Unknown whether inherited from v1/upstream or engine-side. Hypothesis to check: per-shot `load::<PackedScene>` of `impact_effect.tscn`/`bullet.tscn` plus shader recompilation once the last blast/bullet is freed and the resource leaves the cache; a preloaded `OnReady<Gd<PackedScene>>` (already planned for V2-C/V2-D) may remove it. Reproduce on `v1` and on the GDScript original before attributing it to v2 | recorded at the user's request for review at the close of v2 — not acted on now. V2-C checkpoint 1 (2026-09-17): after preloading `bullet.tscn` in `player.rs` (`c3e8d8b`), the user observed the first-shot hitch "don't appear to be present anymore" for the player's bullets (not rigorously measured). V2-D checkpoint 1 (2026-09-17): after preloading `impact_effect.tscn` in `red_robot.rs` (`035604d`), the user confirmed "the FPS drop when shooting, from both the robot and the player are completely resolved, no more FPS drop when firing new rounds" | done — both halves confirmed by the user at the V2-C (player bullet, c3e8d8b) and V2-D (robot laser, 035604d) checkpoints: preloading the scenes removed the first-shot hitch; root cause per the hypothesis (per-shot `load` + shader recompilation once the resource left the cache) |
| 29 | `player.rs` — user observation during the V2-C checkpoint 1, 2026-09-17 | Investigate an occasional FPS drop noticed during play-testing after this milestone's `player.rs` changes ("I feel a FPS drop sometimes, don't know if it is related"). Intermittent, not yet reproduced on demand; relationship to this milestone's changes (preloaded `bullet_scene`, `InputFrame` snapshot, `AnimPlan`-driven `apply_anim`) unconfirmed — needs reproduction steps and profiling before attributing it to any specific change | recorded at the user's request for review at the close of v2 — not acted on now | open — needs reproduction; post-v2 review candidate |
| 30 | `door/door.tscn` — found while validating milestone V2-C's parity harness case (c), confirmed by the user during checkpoint 2, 2026-09-17 | `door.tscn` is never instanced in `level.tscn` in either `v1`/`v2` or the original GDScript demo (an orphaned, untested asset), and even if manually placed today it would not open for the player: the `Door` (Area3D) node's default `collision_mask = 1` does not include the player's `collision_layer = 6` (Godot's area-detects-body rule is one-directional, `area.collision_mask & body.collision_layer`). The door's own dispatch logic (`door.rs`'s `pure::on_body`) is correct and unit-tested; only the scene's collision mask blocks it. Minimal fix: `collision_mask = 7` on the `Door` node in `door.tscn` (covers the player's layer 6 and the robot's layer 3, the latter already incidentally working today) — a scene-only property change, no `door.rs` edit needed | user explicitly deferred the fix at V2-C checkpoint 2 ("Leaves as new backlog, the rest is working") | open — deferred by user request; post-v2 review candidate |
| 31 | `enemies/red_robot/red_robot.gd` (port 2) — found during milestone V2-D's planning (research.md R2) | Reset `aim_preparing`/`shoot_countdown`/`aim_countdown` when the player enters the detection area (`_on_area_body_entered` → `Approach`) instead of resuming with the stale values left by the previous state; once fixed, the internal state can become an enum with per-variant counters (spec 009 FR-002's originally-sketched shape) | `v1` quirk kept for parity in V2-D — the counters outlive `Idle`, so a per-state enum cannot represent `v1` faithfully today | done (spec 013 FR-023, V3-C commit 3: `ecs/apply.rs`'s `RobotPlayerSeen { Some }` arm applies `resume_approach_reset` to `aim_preparing`/`shoot_countdown` on detection-area entry, then `Approach` — `aim_countdown` untouched, as `resume_approach` never reset it; the per-variant enum stays open) |
| 32 | `level/level.rs` (`spawn_robot`/`_respawn_robot`) — user observation during the V2-D checkpoint 1, 2026-09-17 | Investigate the FPS drop when a defeated robot respawns in the environment (15 s timer → `spawn_robot` → `load::<PackedScene>("red_robot.tscn")` + `instantiate` on the main thread, plus the robot's own `#[init(val = load(impact_effect.tscn))]` and three parts' preloads at instantiation); candidate: preload `red_robot.tscn` once in `Level` and/or spread the spawn cost | recorded at the user's request — not acted on now; `level.rs` is V2-E | done (a86b13f) — user confirmed at the V2-E checkpoint 1 (2026-09-18): preloading `red_robot.tscn` in `Level` removed the respawn hitch |
| 33 | `settings/graphics.rs` `GraphicsSettings::default_for` — user decision, 2026-09-18, after v2 closed | v2 ships higher-quality graphics defaults than the original demo: VSync Disabled, MSAA 8×, SMAA, SDFGI High, SSAO High, SSIL High (v1/original: VSync Enabled, MSAA off, SSAA off, VoxelGI Low, SSAO Medium, SSIL off). Only the DEFAULTS change — a `settings.ini` written by v1 is still read as-is; the menu, wire format and `plan()` are untouched | Deliberate, user-authorized deviation from v1 parity for the reference template; recorded so the parity harness's "defaults" case is known to differ from `v1` from this commit on | done (see the commit that changed `default_for`) |
