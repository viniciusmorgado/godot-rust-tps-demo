# v3 trade-offs — engine touch points

Where the ECS abstraction touches the engine (constitution 1.5.1, Principle I v3 and Principle
III "ECS shape"). One row per touch point, added in the commit that introduces it.

| Module | What the engine owns | Why the ECS cannot hide it | How the sync layer handles it | Location |
|---|---|---|---|---|
| `part`, `level`, `red_robot` | Scene instancing: `PackedScene::instantiate` + `add_child` (and the `ready` that follows) | The node must exist before it can be a view of an entity; instancing stays in the v2 node code of the spawner (Principle I v3 scope: `level` is excluded from the ECS, `part`/`red_robot` are later milestones) | The spawned node's bridge pushes `InboundEvent::Register` from its own `ready`; the drain of the next schedule run spawns the entity (`ecs::apply_register`), so the spawner never touches the World | `part.rs:116-117` (preload) / `:196-205` (instantiate in `destroy`), `red_robot.rs:423-425`, `level.rs`'s prefab spawner; `ecs.rs::apply_register` |
| `part_disappear`, `blast` | Frame time (`_process` delta) and the `SceneTreeTimer`s v2 awaited | The ECS cannot hide time itself: a tick timer can only be as exact as the delta the engine hands the driver | `EcsWorld::process` writes `FrameDelta` before the frame schedule; the `Timer` component reproduces `SceneTree::process_timers`' subtractive arithmetic (`time_left -= dt`, fires when `<= 0`, research.md R5) so it expires on the same step as the `SceneTreeTimer` it replaces; `DisappearPhase::advance` steps it in `Phase::Gameplay`, `sync_out_puff`/`sync_out_remove` apply the outcome | `ecs/timer.rs`, `part_disappear/system.rs`, `ecs.rs::sync_out_puff` |
| `blast` | Animation playback and its end: `animation_finished` is emitted by the `AnimationPlayer`'s own idle processing | The animation's end is engine time, not tick time — the ECS cannot compute when the engine's own animation will finish | The bridge receives the child's signal through a typed `connect_other` into a `#[func]` that only pushes `BlastAnimationFinished`; the drain (`ecs/apply.rs`) resolves the id and inserts `Remove`; `sync_out_remove` `queue_free()`s the node in the same frame's `SyncOut` | `blast.rs`, `ecs/apply.rs::apply_non_register`, `ecs.rs::sync_out_remove` |
| `blast` | The camera's global transform, and `look_at` (`Basis::looking_at`, engine-backed under the 1.4.1 rule) | The value the decision needs lives in the engine and changes every frame the camera moves; the write is an engine-backed rotation, so a pure step would have nothing to compute | Sync-only entity (NO gameplay system): `sync_in_blast` reads the cached camera's origin once per distinct camera per frame into `LookTarget` with `set_if_neq`; `sync_out_blast` calls `look_at` on `Changed<LookTarget>`. Measured explicit engine calls per blast per frame, by reading the two systems: `is_instance_valid` (camera) + `get_global_transform` + `look_at` = 3, v2's count (`blast.rs:42-47`), plus the FR-009 root validity sweep for a lone blast; the camera calls are shared, so ≤ 3 + sweep from two concurrent blasts — FR-025 as amended | `ecs.rs::sync_in_blast`, `ecs.rs::sync_out_blast` |
| `player` | Root motion: the displacement/rotation the `AnimationTree` accumulated since its last advance (`get_root_motion_position/rotation`) | The animation drives the body: a pure step cannot know how far the walk cycle moved the model this step | `orient_and_anim` (`EngineQueryOrient`) reads it once per step into `RootMotion` (never while airborne, v2 `player.rs:258`); `tick_integrate` (`GameplayIntegrate`) integrates it with `integrate_root_motion` unchanged | `player/sync.rs::orient_and_anim` |
| `player` | `move_and_slide` and the post-move origin (`CharacterBody3D`) | The body's collision response is the physics server's answer; the respawn decision needs the origin AFTER it | `move_body` (`EngineQueryMove`) sets the velocity computed by `tick_integrate`, calls `move_and_slide`, reads `origin.y` into `BodyState`; `tick_settle` (`GameplaySettle`) decides `should_respawn`; `sync_out_player` applies the reset | `player/sync.rs::move_body` |
| `player_input` | The crosshair raycast (`PhysicsDirectSpaceState3D::intersect_ray` from the camera's projected ray), AFTER this frame's camera rotation | The ray origin/direction come from the engine-computed camera transform through the `SpringArm3D` chain; the rotation writes must precede it in the same frame (v2 `player_input.rs:94-95` before `:117-119`) | `camera_and_ray` (frame `EngineQuery`) applies the camera deltas in v2's order (mouse first, then controller, `clamp_pitch` on the live rotation), THEN raycasts when shooting; `shoot_target` is projected into the input node by `sync_out_input` | `player_input/sync.rs::camera_and_ray` |
| `player` | Engine-backed rotation math: `Quaternion::slerp`, `Basis::looking_at` (CLAUDE.md's builtin-math rule) | Both go through the engine's builtin methods, so they cannot run in a pure system | `tick_decide` emits the target (`OrientTarget::Camera(q)` / `Walk(target)` from `walk_target`, pure); `orient_and_anim` (`EngineQueryOrient`) slerps the entity's `Orientation` toward it (v2 `player.rs:261-264`, `:296-300`) | `player/sync.rs::orient_and_anim` |
| `player` | Bullet instancing from the tick (`PackedScene::instantiate` + `add_child` under the player's parent, `look_at`, `add_collision_exception_with`) | The bullet node must exist to be shot; its origin is `ShootFrom`'s PRE-move transform (v2 `player.rs:275` precedes `:322`), so the spawn cannot wait for `SyncOut` | `tick_decide` decides `shoot` (`shooting && cooldown_left == 0.0`); `orient_and_anim` spawns the bullet before `EngineQueryMove` runs `move_and_slide`; `bullet.rs` stays v2 code (its bridge is a later milestone) | `player/sync.rs::orient_and_anim` |
| `player` | The `AnimationTree`'s own processing (advance, blend, root-motion accumulation) and its parameter writes | v2's tick ran BEFORE the tree's PHYSICS processing (tree order), reading the previous step's root motion and writing this step's parameters; the driver at `i32::MAX` runs AFTER it, so a PHYSICS-mode tree would give the tick each step's root motion one step early (research R1: `M(n).rm == S(n+1).rm` at every step, positions diverging from step 13) | `player.tscn:592` `callback_mode_process = 2` (MANUAL, the milestone's one scene edit); `sync_out_player` writes the parameters (`apply_anim`, v1's per-variant order — moved out of `EngineQueryOrient`, where they answered nothing) and then, LAST, `advance(FixedDelta)` for EVERY player entity — proven identical to v2 step by step (R1's runs (i) vs (iii): no differences) | `player/sync.rs::sync_out_player`, `oxide-godot/player/player.tscn:592` |
| `player`, `player_input` | Replication (`MultiplayerSynchronizer`) of node properties, sampled in `SceneMultiplayer::poll()` at the top of `SceneTree::process` (after the physics steps, before the frame run, once per rendered frame — research R2) | The wire format is the node's exported fields, not the components; only the engine samples and applies them | Projection: fixed `sync_out_player` writes `Player.motion`/`current_animation` on `Simulates`; frame `sync_out_input` writes `InputSynchronizer.{aiming, shoot_target, motion, shooting}` on `OwnsInput`; fixed `sync_in_player` reads the input node's fields into `InputFrameC` on every peer and, on non-`Simulates`, the replay fields into `ReplayState`; `CameraBase`/`CameraRot` rotations and the transforms replicate from the node values the sync layer writes (contracts/player-entity.md §2) | `player/sync.rs::sync_in_player`/`sync_out_player`, `player_input/sync.rs::sync_out_input` |
| `player` | RPC timing: `jump`/`land`/`shoot` are `#[rpc(authority, call_remote, unreliable)]` (v2: `call_local`) | v2 applied their local effects INLINE in the physics step through `call_local`; a v3 handler could only push an event consumed by the frame run — `FireCooldown` one frame late, shoot trauma after `shake_decide`, and a transient animation write that replication can sample (R2) | Option (b), spec FR-004: the `Simulates` entity's fixed `SyncOut` applies the local effects itself, in v2's order (`Land` sound, `Jump` sound, `Shoot` particles/cooldown/sound/trauma), then sends the RPCs, which reach remote peers only; their handlers push `PlayerFx` for the frame run's `apply_player_fx` (commit 3). Names and signatures unchanged; the attribute is the one deviation | `player.rs` (handlers), `player/sync.rs::sync_out_player` |
| `camera_noise_shake` | The noise samples (`FastNoiseLite::get_noise_1d`, three per frame while `Trauma > 0`, research R8) and the camera's rotation | The noise generator is an engine resource seeded at the camera's init (`randi()` in v2's call order, seeded parity); its samples feed only a write, no mid-tick decision — so the shake is a sync-only pair, not an `EngineQuery` member (analyze 2026-09-18; V3-A's blast precedent) | `shake_decide` (`Gameplay`, pure: `decay`, `advance_time`, `shake` on the player entity's `Trauma`/`ShakeTime` → `ShakePending`); `sync_out_shake` (`SyncOut`) samples the three noises, computes `offsets` (pure, called from glue) and writes `camera.set_rotation(start_rotation + offset)`; trauma arrives through `AddTrauma` (`hit`, `add_camera_shake_trauma`) and the `Shoot` effects, applied by the drain or by `sync_out_player` so the same iteration's `shake_decide` sees it | `camera_noise_shake/system.rs::shake_decide`, `camera_noise_shake/sync.rs::sync_out_shake`, `ecs/apply.rs` |
| `bullet` | `move_and_collide` and the collider resolved into a `HitKind` (`KinematicCollision3D::get_collider`, `hittable::kind_of`) | The displacement's collision response and the collider are the physics server's answers, and `bullet_settle` decides on them (hit target, collision off, the backlog #13 single explode, `Exploded`); the collider is resolved once, at the query, into an id-carrying value a component may hold (no `Gd`) | `move_bullet` (`EngineQueryMove`) moves along `-dt · VELOCITY · basis_z` (v2 `bullet.rs:111-116`) into `Collided { hit, collided }`; `bullet_settle` (`GameplaySettle`) sets the intents and writes `RobotHitLocal` for a robot; `sync_out_bullet` (`SyncOut`) plays the explosion, calls `HitKind::rpc_hit` (the by-name RPC re-fetched by id), disables the shape and sends `explode` (`call_remote`) | `bullet/sync.rs::move_bullet`, `bullet/system.rs::bullet_settle` |
| `part` | The `RigidBody3D` part simulated and replicated by the engine (freeze, velocities, collisions, transform), and the fade written through the node's `#[var]` setter | The body's motion after the explosion is the physics server's; the `fade_value` projection is replicated to clients and its setter writes the shader parameter inside the bridge | `part_phase_tick` (frame `Gameplay`, pure: `Waiting(Timer)` → `Fading { counter }` → `Destroyed(Timer)` with `fade_curve`/`should_destroy`, V3-A's `Timer` arithmetic for v2's two `SceneTreeTimer`s) decides; `sync_out_part` (frame `SyncOut`) writes the fade through `root.bind_mut().set_fade_value` on `Simulates`; the explosion's node writes come from the robot's death branch (row below); the client keeps `Attached` and gets the fade by replication | `part/system.rs::part_phase_tick`, `part/sync.rs::sync_out_part` |
| `part` | The puff (`part_disappear.tscn`) instanced under the robot's parent (`puff_parent`, backlog #15) at the part's origin | A node must be instanced and placed; it registers itself as V3-A's puff entity | `sync_out_part`: on the destroy intent (server) and on the remote `PartFx::Destroy` (clients, `call_remote`), followed by `rpc("destroy")` on the server and by the entity's own `Destroyed(Timer(0.2))` on the client, so every peer frees its node through `Remove`/`sync_out_remove` (the parts are scene children of the spawned robot, never despawned by the spawner — research R7) | `part/sync.rs::sync_out_part`, `part/sync.rs::puff_parent` |
| `red_robot` | The raycasts (`PhysicsDirectSpaceState3D::intersect_ray` with the robot's RID excluded): the pre-check/aim ray to the player and the shoot ray along the bone's Y for 1000 m; the blast (`impact_effect.tscn`) instanced under the tree root at the shoot hit | The physics server answers mid-tick and `model::step` decides on the answer (`sees_player`); the shoot answer sets the clip length, the ember and the blast position | `robot_decide` (`Gameplay`) emits `raycast`/`shoot` intents with v2's lazy gates (facing + countdown about to expire); `robot_query` (`EngineQueryOrient`) raycasts into `RaycastAnswers { sees_player, shot: ShotResult { max_dist, hit } }`; `robot_step_and_animate` (`GameplayIntegrate`) steps the model on it; `sync_out_robot` writes the clip, the ember, instances the blast and arms `PendingTrauma` when the collider is the tracked player | `red_robot/sync.rs::robot_query`, `red_robot/sync.rs::sync_out_robot` |
| `red_robot` | The laser `RayCast3D` child (`is_colliding`, `get_collision_point`) read at `SyncIn` | The ray is `enabled = false` in the scene (`red_robot.tscn:10764`) but the shoot animation enables it (track 14, `:10399`), so during Aim/Shooting it is LIVE; research R8's rule: an engine-updated child (`RayCast3D`, a PHYSICS-mode tree) updates after its parent's priority-0 callback and before the `i32::MAX` driver, so a `SyncIn` read is one step NEWER than v2's read inside the parent (harness (d): the clip started one frame earlier and differed in the third decimal — v2 paired the current `ray_from` origin with the previous step's collision point) | `sync_in_robot` reads the ray every run into `LaserBuffer` and fills `RobotFrame.laser_colliding`/`laser_point` from the PREVIOUS run's buffer (R8's one-step buffer); `robot_decide` derives `clip` from the snapshot — harness (d) identical with the buffer | `red_robot/sync.rs::sync_in_robot`, `ecs/markers.rs::LaserBuffer`, `red_robot/system.rs::robot_decide` |
| `red_robot` | The second `AnimationTree` (advance, blend, root-motion accumulation, the hit one-shots) and its parameter writes; `parameters/aim/blend_position` no longer read back from the tree | As the player's: the driver at `i32::MAX` runs after a PHYSICS-mode tree's own processing, so it would read each step's root motion one step early (research R1: `M(n).rm == S(n+1).rm` — M12 0.000016 = S13, M13 0.000032 = S14, M200 0.014937 = S201; v2 PHYSICS == v3 MANUAL + advance, 601 lines, no differences). The tree returns what the tick wrote for a Vector2 parameter, so `AimBlend` (a component stepped by `aim_blend_step`) is behavior-identical to v2's per-step `get` (spec FR-016) | `red_robot.tscn:10782` `callback_mode_process = 2` (MANUAL, the milestone's one scene edit); `sync_out_robot` writes `transition_request`, `aiming/blend_amount`, `aim/blend_position` from the `AnimDecision` (decided on the POST-step state, v2's `animate` after `step`) then, LAST, `advance(FixedDelta)` for every live robot on every peer — skipped in the run the robot dies (v2 set the tree inactive there; `AnimationMixer::advance` ignores `active`); `robot_query` reads the root motion the previous run's `advance` produced | `red_robot/sync.rs::sync_out_robot`, `red_robot/sync.rs::robot_query`, `oxide-godot/enemies/red_robot/red_robot.tscn:10782` |
| `red_robot` | Engine RNG draws (`randi()` for the hit reaction, `randf()` ×4 per part: the angular velocity and the wait) | Seeded parity (`seed(1)`) requires the same draws in the same order as v2 (`red_robot.rs:285`, then `part.rs:173-175` per part in scene order `PartShield1`, `PartShield2`, `PartHead`); research R2 proved the thirteen draws reproduce | The draws happen in glue only, in v2's order: `sync_out_robot`'s hit reaction, then the death branch's `explode_parts` (the cross-entity explosion: the three `Gd<Part>` cloned out of the robot's entry, each part's own `NodeHandles` entry for `synchronizer`/`col1`/`col2`, the part ENTITY's phase set through `EntityIndex` + `Query<&mut PartPhase, Without<RobotTag>>`, research R5); the pure `random_angular_velocity`/`wait_time` take the sampled values; the remote death on a client draws only its own reactions | `red_robot/sync.rs::sync_out_robot`, `red_robot/sync.rs::explode_parts` |
| `red_robot` | The `exploded` signal (`#[signal]`), connected by v2's `level.rs` to `_respawn_robot` | The level stays v2 code (constitution 1.5.2) and listens to the node's signal | `sync_out_robot`'s death branch (and the remote death in `sync_out_robot_frame`) emits `root.signals().exploded().emit()` after the explosion sound (v2 `:306-307`); the respawned robot's `ready` pushes its own `Register` | `red_robot/sync.rs::sync_out_robot`, `red_robot/sync.rs::sync_out_robot_frame` |
| `red_robot` | `move_and_slide` (`CharacterBody3D`) with the velocity of `integrate_root_motion` / `idle_velocity`; ordered BEFORE the bullet's `move_and_collide` | The body's collision response is the physics server's; in v2 the robots (spawned under `SpawnedNodes` at level start, `level.rs:138`) precede the bullets in tree order, so the robot moved before the bullet collided with it every step — bevy leaves two systems of one set unordered, so the order is pinned; v2's exception (a robot respawned after a bullet already existed moved AFTER that bullet) is not reproduced | `move_robot` (`EngineQueryMove`, `.before(move_bullet)`) sets the velocity, the up direction and calls `move_and_slide`; `sync_out_robot` then writes the model basis (`set_global_basis`, v2 `:257-258`) | `red_robot/sync.rs::move_robot`, `ecs.rs::add_engine_systems` |
| `bullet`, `part`, `red_robot` | RPC timing: the bullet's `explode`, the part's `destroy`, the robot's `hit` and `play_shoot` are `#[rpc(authority, call_remote, unreliable)]` (v2: `call_local`); the robot's LOCAL hit travels as `Messages<RobotHitLocal>` inside the fixed run | v2 applied the local effects inline through `call_local`; a v3 handler could only push an event for the NEXT run. The robot's hit is the one cross-entity case: the bullet's `GameplaySettle` writes the message, the robot's `robot_hit_apply` (same set, `.after(bullet_settle)`) reads it in the SAME run (spec option (B), research R4: exactly once; an unread message is dropped after two updates) — the death and the parts' first moving step land on v2's step. `Part::explode` has no caller and is removed | The simulating peer's `SyncOut` applies the local effects itself (explosion, puff, reaction/death) then sends the RPC; the remote handlers push `BulletFx`/`PartFx`/`RobotHit`/`RobotFx` for their peer's frame `SyncOut` — the client runs v2's per-peer `hit` logic on its OWN `Health` (`health`/`dead` are spawn-only replicated). Names and signatures unchanged; the attribute is the deviation | `bullet.rs`, `part.rs`, `red_robot.rs` (handlers), `bullet/system.rs::bullet_settle`, `red_robot/system.rs::robot_hit_apply`, `red_robot/sync.rs::sync_out_robot_frame` |
